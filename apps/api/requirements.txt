fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic-settings==2.7.1
sqlalchemy[asyncio]==2.0.36
aiosqlite==0.20.0
alembic==1.14.0
redis==5.2.1
httpx==0.28.1
pytest==8.3.4
pytest-asyncio==0.25.0
asyncpg==0.30.0
ruff==0.9.6
greenlet==3.5.5
# Real (non-scripted) person detection (AI_DETECTOR_BACKEND=opencv, SPEC 13):
# HOG+SVM pedestrian detector bundled in the wheel itself, no external model
# download needed. No prebuilt opencv wheel exists for Windows ARM64, so
# local dev on that platform stays on the mock backend; CI (ubuntu-latest)
# and the deployed Linux container images install and use it normally.
numpy==2.4.4
opencv-python-headless==4.11.0.86 ; platform_system != "Windows" or platform_machine != "ARM64"
